---
// CTA.astro — Call to action with email form
---

<section id="cta" class="cta-section">
  <div class="container">
    <div class="cta-inner">
      <span class="section-label">// 007 — Let's talk</span>
      <h2 class="cta-heading">
        Punya ide?<br />
        <em>Mari bangun bersama.</em>
      </h2>
      <p class="cta-sub">
        Tulis email Anda di bawah. Kami siap mulai dengan diskusi singkat minggu ini.
      </p>

      <form class="cta-form" id="cta-form">
        <input
          type="email"
          name="email"
          id="cta-email"
          class="cta-input"
          placeholder="email@anda.com"
          required
          autocomplete="email"
        />
        <button type="submit" class="btn-primary cta-submit">
          Kirim brief
        </button>
      </form>

      <div class="cta-success" id="cta-success">
        <span class="cta-success-icon">✓</span>
        <span class="cta-success-text">Terkirim. Kami akan menghubungi Anda dalam 24 jam.</span>
      </div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding: var(--sp-2xl) 0; /* φ⁴ */
    background-color: var(--bg2);
    border-top: 0.5px solid var(--border);
    border-bottom: 0.5px solid var(--border);
  }

  .cta-inner {
    text-align: center;
    max-width: 618px; /* 1000 × φ⁻¹ */
    margin: 0 auto;
  }

  .cta-inner .section-label {
    display: block;
  }

  .cta-heading {
    margin-bottom: var(--sp-sm);
  }

  .cta-sub {
    font-size: 1rem;
    color: var(--fg2);
    margin-bottom: var(--sp-lg);
  }

  .cta-form {
    display: flex;
    gap: 0;
    max-width: 500px;
    margin: 0 auto;
  }

  .cta-input {
    flex: 1;
    font-family: var(--font-mono);
    font-size: 0.85rem;
    padding: 0.875rem 1.25rem;
    min-height: 48px;
    background-color: var(--bg);
    color: var(--fg);
    border: 0.5px solid var(--border2);
    border-radius: 0;
    outline: none;
    transition: border-color 0.25s var(--ease-out-expo), box-shadow 0.25s ease;
  }

  .cta-input::placeholder {
    color: var(--fg3);
  }

  .cta-input:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 2px rgba(200, 255, 0, 0.08);
  }

  .cta-submit {
    flex-shrink: 0;
    border: 0.5px solid var(--accent);
  }

  .cta-success {
    display: none;
    align-items: center;
    justify-content: center;
    gap: var(--sp-xs);
    margin-top: var(--sp-md);
    padding: var(--sp-sm);
    border: 0.5px solid var(--accent);
    animation: fadeInUp 0.5s var(--ease-out-expo) forwards;
  }

  .cta-success.is-visible {
    display: flex;
  }

  .cta-success-icon {
    color: var(--accent);
    font-size: 1rem;
    font-weight: 700;
  }

  .cta-success-text {
    font-family: var(--font-mono);
    font-size: 0.82rem;
    color: var(--accent);
  }

  @media (max-width: 540px) {
    .cta-form {
      flex-direction: column;
      gap: var(--sp-xs); /* golden ratio gap when stacked */
    }

    .cta-submit {
      width: 100%;
      justify-content: center;
    }
  }

  @media (max-width: 768px) {
    .cta-section {
      padding: var(--sp-xl) 0;
    }
  }
</style>

<script>
  const form = document.getElementById('cta-form') as HTMLFormElement | null;
  const success = document.getElementById('cta-success');

  if (form && success) {
    form.addEventListener('submit', (e) => {
      e.preventDefault();
      form.style.display = 'none';
      success.classList.add('is-visible');
    });
  }
</script>
